#include "merge_calkowite.hpp"

void merge_calkowite(vector <int> &tc)
{
 clock_t stop, start = clock();
   
   
 
    stop = clock();
    cout << "\n merge_calkowite czas: " << (stop - start)/CLOCKS_PER_SEC;
}
