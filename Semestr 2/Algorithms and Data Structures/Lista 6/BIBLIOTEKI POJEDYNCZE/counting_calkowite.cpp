#include "counting_calkowite.hpp"

void counting_calkowite(vector <int> &tc)
{
    clock_t stop, start = clock();
   
   
   
 
    stop = clock();
    cout << "\n counting_calkowite czas: " <<(stop - start)/CLOCKS_PER_SEC;   
}
